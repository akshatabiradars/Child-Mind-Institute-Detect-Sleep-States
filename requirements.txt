# Runtime dependencies for the Detect Sleep States app.
#
# Designed for a robust install on Streamlit Community Cloud (and locally) with
# PYTHON 3.11 (see .python-version). Only the libraries the app actually imports
# are listed.
#
# IMPORTANT: scikit-learn and xgboost are PINNED to the versions that created
# pipeline_v2.pkl / pipeline_01_legacy.pkl. A newer scikit-learn can fail to
# unpickle the model's ColumnTransformer, so do not relax these two.
#
# Everything else uses minimum-version floors so the platform installs current,
# wheel-available builds (no slow/fragile source compilation).

# --- model (pinned: must match the serialized pipeline) ---
scikit-learn==1.3.2
xgboost==2.0.2

# --- web app + data ---
# Floor raised to 1.45 for the native theming keys used in .streamlit/config.toml
# (font-from-URL, headingFont, borderColor, baseRadius, showWidgetBorder). These
# give a polished, CSS-light "StockPeers"-style finish.
streamlit>=1.45,<2
pandas>=2.1,<2.3
numpy>=1.26,<2
plotly>=5.18
matplotlib>=3.8
Pillow>=10
pyarrow>=14          # enables .parquet uploads via pandas.read_parquet
