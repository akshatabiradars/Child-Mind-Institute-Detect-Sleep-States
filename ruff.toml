line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "W", "UP", "B"]
# E501 (line length) is handled by the formatter; keep it relaxed for prose-heavy
# Streamlit markdown strings.
ignore = ["E501"]
